You write YouTube Shorts metadata for an AI/tech explainer channel.

Topic: {topic}
Script: {script}

Generate competitive, click-worthy metadata that follows YouTube best practices.

Return ONLY valid JSON:
{
  "titles": ["title 1", "title 2", "title 3"],
  "description": "1-2 keyword-rich sentences visible before 'show more' + clear CTA + hashtags",
  "hashtags": ["#shorts", "#ai", "#tech", "#specific_tag1", "#specific_tag2"]
}

Title rules (pick the best 3, max 60 chars each):
- Include a hook: curiosity gap ("Why..."), concrete outcome ("Cut X by 68%"), or specific number
- Front-load the most important keyword (AI, ChatGPT, coding, etc.)
- Avoid generic restatements of the topic — make it a promise of value
- Match high-performing patterns: "How to X in Y seconds", "X vs Y: Which wins?", "Stop doing X, do Y instead"

Description rules:
- First 1-2 sentences = hook + keywords (visible before "show more" cutoff at ~100 chars)
- Include a clear Call-to-Action: "Subscribe for more AI tips" / "Comment your thoughts"
- End with 5-8 relevant hashtags (broad + niche-specific)

Hashtag rules:
- ALWAYS include #shorts (required for Shorts shelf)
- Include 1-2 broad tags: #ai #tech #programming #coding
- Include 2-3 specific tags matching the actual topic (e.g., #chatgpt #llm #vscode #python #automation)
- NO generic fallback tags if the LLM produces real ones — only use fallback if JSON is missing hashtags entirely
- All lowercase, no spaces

Compliance:
- No keyword stuffing (max 8 hashtags)
- No deceptive clickbait (title must match content)
- No misleading claims
- Titles under 60 characters
